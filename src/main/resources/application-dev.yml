spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
  #  config:
  #    import: optional:file:.env[.properties]  # This tells Spring to load the .env file and [.properties] to tell Spring to parse the file using the standard Key=Value property format
  jpa:
    show-sql: true
#  mail:
#    host: ${SMTP_HOST} # smtp.gmail.com
#    port: ${SMTP_PORT} # Standard port for TLS/secure email
#    username: ${SMTP_USERNAME} # Gmail address
#    password: ${SMTP_PASSWORD} # App Password (No spaces!)
#    properties:
#      mail:
#        smtp:
#          auth: true
#          ssl:
#            enable: true
  jwt:
    secret-key: ${JWT_SECRET}
    access-token-validity-seconds: 604800 # 604800 is 7 days in seconds
    refresh-token-validity-seconds: 604800 # 604800 is 7 days in seconds
app:
  base-url: "http://localhost:8080"